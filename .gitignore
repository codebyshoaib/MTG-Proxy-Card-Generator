.env
*.sqlite3
__pycache__/
.venv/
node_modules/
.next/
out/
spike-out/
generated/
.DS_Store
art-out/
# Scryfall card images a spike downloaded to measure against. 46MB, refetchable, and this repo is
# handed to the client at Milestone 3.
cards-cache/
docs/
# What a batch PRODUCED. 10MB a card, and two batches already outweigh the whole repo. The spec
# beside them — backend/packs/*.json — IS tracked, and it plus `manage.py pack` reproduces the
# batch, which is the point of the spec being a committed file.
backend/packs/*/
# Local Render CLI binary if downloaded for deploy; do not commit.
.render-cli-bin
