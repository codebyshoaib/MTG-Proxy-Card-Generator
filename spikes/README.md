One-off scripts that produced the measured evidence quoted in BUILD-SPEC.
Not part of the running system. They read GEMINI_API_KEY from a .env in the
directory they are run from and write to spike-out/ or evidence-*/.
